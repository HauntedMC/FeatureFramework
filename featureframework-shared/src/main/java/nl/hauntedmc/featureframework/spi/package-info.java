/**
 * Cross-module framework service-provider interfaces and implementation seams.
 *
 * <p>Types in this namespace are public only because the Paper and Velocity artifacts are separate
 * Maven modules. They are framework plumbing, not the long-term consumer API surface. Consumers
 * should prefer the stable contracts in {@code api}, {@code feature}, {@code host}, {@code service},
 * {@code config}, {@code localization}, and {@code toolkit}.</p>
 */
package nl.hauntedmc.featureframework.spi;

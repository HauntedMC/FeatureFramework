# 01 — Simple Velocity feature

The ready-to-use Velocity host receives the plugin instance, `ProxyServer`, component logger, data directory, API root, and feature collection explicitly.

`ProxyWelcomeFeature.java` is one managed feature. `ProxyPlugin.java` shows the composition shape you would call from your Velocity plugin's initialization lifecycle.
